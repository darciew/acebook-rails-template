<h2 class="form-title">Edit Wall Post</h2>
<div class='edit-wallpost-form forms'>
<%= form_for [@user, @wallpost] do |f| %>
    <%= f.text_area :text %>
  <%= f.hidden_field :sender_id, value: current_user.id %>
    <%= f.submit 'Update Wall Post' %>
<% end %>
</div>
